import type { Meta, StoryObj } from '@storybook/angular';
import { IconArrowDownTrayComponent } from './icon-arrow-down-tray.component';

const meta: Meta<IconArrowDownTrayComponent> = {
  component: IconArrowDownTrayComponent,
  title: 'IconArrowDownTrayComponent',
};
export default meta;
type Story = StoryObj<IconArrowDownTrayComponent>;

export const Primary: Story = {
  args: {},
};
