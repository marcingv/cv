import { DetailsViewRowLabelDirective } from './details-view-row-label.directive';

describe('DetailsViewRowLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailsViewRowLabelDirective();
    expect(directive).toBeTruthy();
  });
});
