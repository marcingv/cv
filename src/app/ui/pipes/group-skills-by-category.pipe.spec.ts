import { GroupSkillsByCategoryPipe } from './group-skills-by-category.pipe';

describe('GroupSkillsByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupSkillsByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
