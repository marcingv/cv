import { DetailsViewRowValueDirective } from './details-view-row-value.directive';

describe('DetailsViewRowValueDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailsViewRowValueDirective();
    expect(directive).toBeTruthy();
  });
});
