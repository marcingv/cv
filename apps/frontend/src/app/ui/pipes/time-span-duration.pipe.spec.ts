import { TimeSpanDurationPipe } from './time-span-duration.pipe';

describe('TimeSpanDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeSpanDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
